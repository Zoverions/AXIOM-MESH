{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://axiom.invalid/schemas/circle-exchange-v0.schema.json",
  "title": "AXIOM Circle Exchange Update v0",
  "description": "One signed entry in the hash-linked log of one key a Circle member holds, bound to the Circle's genesis, which carries the creator's key. Keys are endorsed by an administrator, rotated by their holder, or revoked from a named counter, each with proof of possession (mesh/src/lib/circle-keys.mjs). Authorship, contiguity, equivocation, key validity, authority rules and the deterministic Circle view are enforced by mesh/src/lib/circle-exchange.mjs. Exchange grants no authority, executes nothing and opens no network connection.",
  "type": "object",
  "additionalProperties": false,
  "required": ["body", "attestation"],
  "properties": {
    "body": {
      "type": "object",
      "additionalProperties": false,
      "required": ["schema", "genesis_digest", "author", "key_id", "counter", "prev_digest", "record_type", "record"],
      "properties": {
        "schema": { "const": "axiom-circle-update.v0" },
        "genesis_digest": { "$ref": "#/$defs/digest" },
        "author": { "type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9_.:-]{0,159}$" },
        "key_id": { "$ref": "#/$defs/digest", "description": "SHA-256 of the signing key's SPKI DER encoding. Each key has its own log." },
        "counter": { "type": "integer", "minimum": 1, "maximum": 4096 },
        "prev_digest": { "oneOf": [{ "type": "null" }, { "$ref": "#/$defs/digest" }] },
        "record_type": { "enum": ["invitation", "membership", "proposal", "task", "appeal", "exit", "export", "ballot", "key_endorsement", "key_rotation", "key_revocation"] },
        "record": { "type": "object" }
      }
    },
    "attestation": {
      "type": "object",
      "additionalProperties": false,
      "required": ["algorithm", "digest", "signature"],
      "properties": {
        "algorithm": { "const": "Ed25519" },
        "digest": { "$ref": "#/$defs/digest" },
        "signature": { "type": "string", "minLength": 1, "maxLength": 1024 }
      }
    }
  },
  "$defs": {
    "digest": { "type": "string", "pattern": "^[a-f0-9]{64}$" }
  },
  "x-axiom-semantic-validator": "mesh/src/lib/circle-exchange.mjs"
}
