# Foundational Execution Pack — 2026-09

**Build:** `0.12.0-dev.3`  
**Audience:** Owner (Zov Erion)  
**Status:** Operating pack only. Changes no capability registry entries and claims no production promotion.

**Doctrine:** Build broadly. Activate deliberately. Expose minimally. Promote only with evidence. Market only what is true.

**Authority path:** Gateway → Hypervisor → Sandbox → Grid.

## Frozen progression (single track)

Work only this sequence until the foundational milestone definition of done is met:

1. **Pilot ops** — authentic controlled pilot evidence package + independent security review intake (`docs/operations/PILOT-EXTERNAL-EVIDENCE-CHECKLIST.md`, `docs/operations/PILOT-DEPLOYMENT-DOSSIER.md`).
2. **AXIOM One + one runtime-adjacent utility** — productize `apps/axiom-one/` and land one least-privilege AI provider under Mesh grants (`docs/operations/AXIOM-ONE-PROVIDER-WEDGE.md`).
3. **One read-only Adapter pin** — select and integrate exactly one maintained external runtime behind Agent Runtime Adapter v1 read-only gates (`docs/operations/RUNTIME-ADAPTER-FIRST-PIN.md`).
4. **Verify standalone** — ship local/static AXIOM Verify MVP (`docs/operations/AXIOM-VERIFY-MVP-SCOPE.md`).

Do not reorder to chase novelty. Parallel lab exploration may continue only as parked research with no claim, no registry change, and no marketing.

## What to park (explicitly)

Park until the frozen progression clears the relevant gate:

- activating the production-unreachable repository-effect / docs-operator path;
- MCP or A2A exposure;
- remote workload execution beyond the read-only adapter pin;
- Circles multi-user pilots;
- Managed Node commercial activation;
- Personal Compute Fabric / Local Trust Plane / wearable implementation beyond docs;
- plural-authority and Agent Contributor Mode consequential effects;
- installer productization beyond INSTALL-001 design (may resume after pilot ops is staffed);
- additional runtime candidates beyond the single first pin;
- Education production-policy activation without Mesh compatibility evidence.

Parked work may keep issue labels `parked-foundational` and must not imply availability.

## Weekly cadence

| Day | Focus |
|---|---|
| Mon | Decision-log review; confirm still on frozen progression; kill or park drift |
| Tue–Wed | Deep work on current stage only (pilot evidence, One/provider, adapter pin, or Verify) |
| Thu | Evidence hygiene: digests, schemas, non-claims, capabilities.json alignment |
| Fri | Demo or written status: what is true, what is not claimed, next gate |
| Continuous | No marketing language that outruns registry or external evidence |

Owner timebox: prefer finishing one gate over starting three. If blocked more than one week on external dependencies (hardware, reviewer), record the blocker and advance only non-claim-expanding prep work on the same stage.

## Decision log template

Copy into `docs/operations/decision-log/` (or owner notebook) as `YYYY-MM-DD-slug.md`:

```text
# Decision: <title>
Date: <YYYY-MM-DD> (America/Toronto)
Owner: Zov Erion
Stage: Pilot ops | One+provider | Adapter pin | Verify | Other(parked)

## Context
<what forced a choice>

## Options considered
1. ...
2. ...

## Decision
<one sentence>

## Evidence referenced
- paths, digests, issue ids, dossier entries

## Explicit non-claims
- ...

## Follow-ups
- [ ] ...

## Revisit when
<condition or date>
```

## Definition of done — foundational milestone

The foundational milestone is met only when all of the following are true. Meeting mechanism drills alone is insufficient.

1. Pilot package accepted for promotion review via pilot:package:verify against an authentic offline package, with production_promoted false still honest until a separate written promotion decision.
2. Independent security review ledger verified for the exact pinned source, image, deployment policy, and pilot configuration (security-review:verify path; live review external).
3. AXIOM One has completed agreed browser, security, accessibility, and package gates for a bounded local product path without claiming general consequential execute.
4. One AI provider under Mesh grants ships with receipts and fail-closed denials; model output does not authorize effects.
5. One read-only Adapter v1 pin selected with immutable upstream commit, parity matrix green, and no MCP, A2A, or remote-exec claim.
6. AXIOM Verify MVP verifies machine receipts and export packages offline; UI or CLI states integrity versus truth.
7. capabilities.json unchanged except by separate, evidence-backed PRs; this pack never self-promotes status.
8. Public claims match PROJECT-STATUS non-claims unless a dated decision explicitly updates them with evidence.

Exit criterion for foundational authority substrate language: lived pilot evidence, independent review intake, one human utility wedge, and Verify — not additional laboratories.
