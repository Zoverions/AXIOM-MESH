# Archived Legacy Runtime Entrypoints

These files were formerly located at the repository root and could appear to
be supported install, container, contract, monitoring, or test entrypoints.
They target the historical multi-language implementation and are retained only
for traceability.

Do not run them as deployment instructions. The supported root commands now
delegate to `mesh/`, and the only active GitHub workflow is
`.github/workflows/kernel.yml`.
