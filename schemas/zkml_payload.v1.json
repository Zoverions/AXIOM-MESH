{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://axiommesh.io/schemas/zkml_payload.v1.json",
  "title": "ZKMLPayload",
  "description": "Represents a Zero-Knowledge Machine Learning payload for verification on the Grid.",
  "type": "object",
  "properties": {
    "model_commitment": {
      "type": "string",
      "description": "Cryptographic commitment to the model parameters."
    },
    "input": {
      "type": "array",
      "items": {
        "type": "number"
      },
      "description": "The input data provided to the model."
    },
    "output": {
      "type": "array",
      "items": {
        "type": "number"
      },
      "description": "The output generated by the model."
    },
    "proof": {
      "type": "string",
      "description": "The cryptographic proof verifying the inference."
    }
  },
  "required": ["model_commitment", "input", "output", "proof"],
  "additionalProperties": false
}
